apiVersion: apps/v1
kind: Deployment
metadata:
  name: companies-mongo-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: companies-mongo
  template:
    metadata:
      labels:
        app: companies-mongo
    spec:
      containers:
        - name: companies-mongo
          image: mongo
---
apiVersion: v1
kind: Service
metadata:
  name: companies-mongo-srv
spec:
  selector:
    app: companies-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017